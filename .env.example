# LLM Configuration for AI Twin (Optional API configs)
# Used specifically in src/app/api/chat/route.ts for the AI twin integration.

# The LLM API Key (e.g. from Nvidia integrations or OpenAI)
LLM_API_KEY=""

# The base URL of the LLM endpoint if not using default Nvidia integration
# e.g., https://api.openai.com/v1/chat/completions
LLM_BASE_URL="https://integrate.api.nvidia.com/v1/chat/completions"

# Override default model. The AI Twin uses tool calling, so the model MUST
# support the OpenAI `tools` parameter (function calling).
# AI_MODEL="deepseek-ai/deepseek-v4-pro-0813"

# Resend contact form delivery
# Verify your sending domain in Resend first, then use an address on that domain.
RESEND_API_KEY="re_your_resend_api_key"
CONTACT_EMAIL_FROM="Portfolio Contact <hello@example.com>"
CONTACT_EMAIL_TO="hello@example.com,team@example.com"
